using System;

public class Video
{
    public string _title;
    public string _author;
    public int _length;

    public string Video(string title, string author, int length)
    {
        Comment comment = new Comment();
    }

    public void AddComments(newComment.Comment)
    {
        List<videoComment> = new List<videoComment>();
        comment.Add(newComment);
    }

    public void DisplayComment()
    {
        string List<string> comment new List<string>();

        foreach (comment c in comments)
        {
            Console.WriteLine($"{_comment}");
        }

    }

}